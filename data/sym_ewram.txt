/* FFTA (USA) EWRAM symbol registry, pret/SA3 style.
 *
 * 0x02000000..0x0203FFFF, 256 KiB. The matched C references a small number of
 * fixed anchors here, mostly parked structs and record buffers.
 *
 * Conventions: `g` prefixes globals; `unk` keeps unidentified storage honest.
 * Everything listed here must also appear in data/symbols.txt (globals the C
 * references) or data/asm_symbols.txt (call targets that are still assembly);
 * the build and tools/elfutil.py consume those two files, this registry is the
 * human-readable layer naming what each address means. Only addresses with
 * retail evidence from execution, tracing, or matched readers belong here.
 */

/* gEwram marks the arena start; several matched readers take a base plus a
 * dynamic record offset into it. */
gEwram = 0x02000000;        /* data/symbols.txt: gUnk_020020D0 etc. anchor
                             * specific parked structs inside the arena */

/* 0x0200918C holds a struct pointer read by sub_08013364; it is not yet a
 * linker symbol because that source still spells it as a cast literal
 * (docs/matching-notes.md). Register it when the source adopts the symbol. */
